
inch를 cm로 변환

프로그램(program = pro(미리) + gram(작성된것))

입력 : prompt() inch단위 숫자
const input = Number(prompt('숫자를 입력해주세요'))

처리 : 1inch -> 2.54cm
const output = input * 2.54

출력 : cm 단위의 숫자
console.log(`${input}inch = ${output}cm입니다.`)