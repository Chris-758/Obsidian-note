
//[ '여러가지', 10, true, 1]

메서드

// 배열 뒤에 요소 추가
.push

// 배열 중간에 요소 추가
.splice(인덱스, 0, 요소)

// 인덱스로 배열 요소 제거
.splice(인덱스, 1)

// 배열 내부에서 값 위치 찾기
.indexof(요소)
원하는 요소가 없을 경우 -1 출력

// indexof() + splice() 활용
const index = a.indexof(요소)
배열이름.splice(index, 1)