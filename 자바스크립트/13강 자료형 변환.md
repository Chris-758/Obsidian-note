
문자열 입력: prompt()
- const a = prompt("아무것이나 입력", "")

불 입력: confirm()

숫자형으로 변환
- const a = Number(prompt('숫자를 입력하시오'))

Not a Number -> NaN
- typeof(NaN) ->


불 -> 문자열
- String(true) -> "true"
- String(false) -> "false"

불 -> 숫자
- Number(true) -> 1 -> 켜져있다, 존재한다

다른 자료형 -> 불
- 5가지 경우를 제외한 모든 경우 true
- 0, NaN, "", null, undefined